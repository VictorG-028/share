# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

The project uses **uv** for the environment and dependencies (declared in `pyproject.toml`; do not use conda or a bare `python` — on this machine bare `python` hits a Windows Store stub). Prefix everything with `uv run`:

```bash
uv sync                       # create .venv + install from uv.lock
uv run python -m pytest       # full suite (config in pytest.ini: pythonpath = src)
uv run python -m pytest src/test/unit/test_validation.py::test_valid_sample_week   # single test
uv run python -m pytest src/test/unit            # only unit tests (also: integration, e2e)
uv run auto-appointment --help                   # the CLI (installed entry point)
uv run auto-appointment --day 26/08/2026         # generate and print; opens nothing
uv add <pkg> / uv add --dev <pkg>                # add a runtime / dev dependency

# Build the Windows executables (no cross-compile; run this on Windows).
# Three of them land in dist/: the main CLI, and one dedicated .exe per
# maintenance flag (same code, different entry point).
uv run pyinstaller --onefile --noconfirm --name auto-appointment --paths src \
  --add-data "src/modules/history/data;modules/history/data" src/main.py
uv run pyinstaller --onefile --noconfirm --name refresh-osi-list --paths src \
  --add-data "src/modules/history/data;modules/history/data" src/refresh_osi_list.py
uv run pyinstaller --onefile --noconfirm --name register-new-osi --paths src \
  --add-data "src/modules/history/data;modules/history/data" src/register_new_osi.py
```

There is no lint step. Runtime deps: `pydantic` (>=2), `websocket-client` (the CDP transport — pure Python, so the PyInstaller bundle stays trivial), and `prompt_toolkit` (the interactive grid-form's terminal UI — a deliberate, larger addition; see the CLI section below). Dev deps: `pytest`, `pyinstaller`. `playwright` was deliberately **not** adopted: it downloads its own browsers, which do not fit in a onefile bundle.

## Architecture

A generator of workday "appointment" punch times (entry / lunch start / lunch end / exit), with a hard separation between **producing numbers** and **driving a browser**. The orchestrator (`src/main.py`) is the only place that wires them together: it loads history, picks a strategy via the factory, generates values **once**, and (future phase) replays the *same* values on each browser controller so the two can be compared on equal input.

Generation uses the GoF **Strategy** pattern:
- **`StaticStrategy`** (the default) — six memorizable sets numbered 1..6, produced by the formula in `modules/strategy/static/generator.py` (`static_times(n)`). `generate_for(day)` maps the weekday to a set: Mon..Fri → 1..5, weekend → 6 (the spare). It does **not** raise on weekends.
- **`NaturalRandomStrategy`** — random times retried until they satisfy `modules/validation/validator.py`, constrained by injected history.

Strategies are selected through `get_strategy(StrategyType, history=...)` in `modules/strategy/__init__.py` (`DEFAULT_STRATEGY = StrategyType.STATIC`).

### Layout convention (deliberate)
- `models/` holds only what is **shared across modules** — currently the `Appointment` pydantic model (`BaseModel`; construct with keyword args, fields validated on creation).
- `modules/` holds implementations grouped by domain; each domain's **ABC lives inside its own module** (`modules/strategy/base.py`, `modules/browser/base.py`), not in `models/`.
- The strategy primitive is `generate_for(day: date)`; `generate_week()` is a helper built on it.
- Not every domain needs a `base.py` ABC — only when there are (or are clearly planned) multiple implementations, as in `modules/browser/` and `modules/strategy/`. `modules/osi_catalog/`, `modules/osi_register/` and `modules/tui/` have exactly one implementation each and deliberately have no ABC.
- **`modules/osi_register/` creates OSIs** (`register-new-osi` / `--register-new-osi`). Pure pieces (`window.py` parses the site's rejection popup into an activity's accepted period, `description.py`, `effort.py`) plus `flow.py`, which drives `SsgOsiFormController` in the user's own order and imports the browser stack lazily. Two things are deliberate: an activity's period is only learned by **saving with dates a year off and reading the rejection** (the site publishes it nowhere; the user accepts the ghost-OSI risk and `discover_window` aborts if such a save is *not* rejected), and the effort is whatever makes the site's own "por Dia Útil (H)" read 8 (probe-fill 40, read, recompute, verify) — no local business-day rule. The real "Gravar" only happens after a printed summary and a confirmation (`--yes` skips).

### Invariants worth preserving
- **Generator must stay aligned with the validator.** Allowed hour ranges live in `validator.py` (`ENTRY_HOURS`, `LUNCH_START_HOURS`, `EXIT_HOURS`); `natural_random/generator.py` imports and samples from them. Changing one side without the other silently makes rules unreachable.
- **Rule 4 windowing has a single source of truth.** `validate()` trims `history` to the last `WINDOW_DAYS - 1` entries (`WINDOW_DAYS = 7`, by count) itself, so callers pass the full history and never diverge. The generator passes its whole history; do not re-window in callers. A comment in `validator.py` documents how the real system differs (centred ±4 calendar days, holiday-aware) from our simpler backward-only by-count window.
- **History is trusted, never validated on load.** `_load_history` parses the CSV but does not enforce the rules — real-world punches may legitimately break them. `history_messy.csv` exists to prove load ≠ validate.
- **Reads are packaged, writes are not.** `paths.bundle_dir()` (`src` in a checkout, the extraction dir when frozen) is where packaged data is read from; everything written — browser profile, holiday cache, the growing history — goes under `paths.user_data_dir()`. A frozen onefile recreates its extraction dir every run, so writing there loses the data silently. `loader.append_appointment` records only punches that actually landed on the site, and refuses the same day twice.
- **`NaturalRandomStrategy` is immutable.** It never mutates its stored history; `generate_week` uses a local accumulator so days in a week stay unique (Rule 4) without side effects.
- **`Appointment.week_day` is derived** from `day.weekday()` (a property, no stored state). Weekday keys: 0 = Monday … 6 = Sunday.
- **`Appointment` is a pydantic `BaseModel`:** it has no positional constructor — always build it with keyword args. Equality compares all fields (incl. `day`/`osi`); `__repr__`/`__str__` are overridden for compact `HH:MM` output.
- **Only the future is a hard block — for *punching*.** SSG cannot accept a timestamp that has not happened yet, so `main.run` returns `None` for `target_day > date.today()`, and `force=True` does **not** bypass it (`force` exists only for weekends and holidays). Today is **not** special-cased: `skip_reason` lets it through like any other day, and whether the SSG actually accepts it — it depends on whether the punched times have already passed in wall-clock terms — is for the SSG itself to decide when `punch()` tries; we do not predict the exact rule (which field, what margin) client-side. This does **not** apply to *reading*: `--refresh-osi-list` reads today's OSI list on purpose (the list is day-filtered by the server, and today's is the freshest), which is a GET that creates nothing.
- **Day eligibility lives in the orchestrator, not the strategies.** `main.run` skips weekends and Brazilian holidays by default; `force=True` punches anyway (used when actually asked to work). The strategies are calendar-agnostic: `StaticStrategy` no longer raises on weekends (it uses spare set 6), `NaturalRandomStrategy` generates regardless.
- **Holiday checks come from `modules/holiday/service.py`.** `is_holiday(date)` resolves national holidays via local cache → BrasilAPI (stdlib `urllib`, no dep) → offline fixed-date fallback. Pass `allow_network=False` to stay offline (tests do). The per-year cache lives under `user_data_dir()/holidays`; movable holidays (Carnaval etc.) are only covered when the API/cache is available, not by the fallback.

### Imports
`pytest.ini` puts `src` on the path, so use absolute imports rooted there (`from models.appointment import Appointment`, `from modules.strategy import get_strategy`). `src/` has **no** `__init__.py` (so pytest roots test packages at `src`); the package tree under `models/`, `modules/`, and `test/` does.

`[tool.hatch.build.targets.wheel] sources = ["src"]` strips the prefix so `main.py`, `models/` and `modules/` land at the wheel root — the same root pytest assumes. That is what keeps those absolute imports working both from a checkout and from an installed wheel; changing it breaks the CLI entry point (`main:cli`).

### CLI
`main.cli()` is the entry point. The default is **dry**: it generates and prints, opening no browser and writing nothing. `--fill` types and verifies; `--save` writes and asks for confirmation first (`--yes` skips), refusing rather than guessing when there is no tty. `--fill` without `--save` rejects more than one day on purpose — filtering the next day re-renders the screen and discards what was typed. `--week` goes through the strategy's `generate_week` rather than looping over `run()`, because `NaturalRandomStrategy` keeps a week's days unique from each other. `--osi` takes the label the site shows, or any text matching exactly one row of that day's list (a bare number still works); without it, the last saved OSI is used and, failing that, the run refuses rather than guessing. `--refresh-osi-list` is read-only — it does only that (probes the live site, writes `osi_catalog.json` under `user_data_dir()`) and ignores every *punching* flag, while accepting the three refresh ones (`--fonte {apontamento,listagem,ambas}`, `--incluir-feriado`, `--incluir-fim-de-semana`), defined once in `main.add_refresh_arguments` and shared with the dedicated `refresh-osi-list` executable so the two can never drift. Exit codes: `0` done, `1` error, `2` nothing to do (a skipped holiday is not a failure).

- **The interactive grid-form triggers from inside `cli()`**, gated on `argv is None and len(sys.argv) == 1 and sys.stdin.isatty()` — *not* textually inside `if __name__ == "__main__":`. The installed console-script stub does `from main import cli; if __name__ == "__main__": sys.exit(cli())` — that dunder-main is the *stub's own*, not `main.py`'s, so a check placed in `main.py`'s own `if __name__ == "__main__":` block would never fire for `uv run auto-appointment`, only for the frozen `.exe` (which really does run `src/main.py` as `__main__`). Gating on `argv is None` inside `cli()` fires correctly for both, and — this is the actual invariant that matters — leaves `main.cli([])` (an explicit empty list, asserted by `test_defaults_to_yesterday`) meaning exactly "yesterday, dry mode" regardless of real `sys.argv`/isatty, since `argv is None` is `False` for an explicit `[]`. Don't move this check into `__main__` — it would silently break the `uv run` path.
- **`refresh-osi-list` has its own screen, gated the same way.** `refresh_osi_list.cli()` opens `run_refresh_tui()` (source spinner + the two toggles) on `argv is None and len(sys.argv) == 1 and sys.stdin.isatty()` — the same reasoning as `main.cli`, including that `cli([])` must keep meaning "all defaults". An empty argv from the screen means exactly that, so callers compare against `None`, never truthiness.
- **`modules/tui/` splits pure state from `prompt_toolkit` glue.** `state.py` (the grid's focus/value logic — day/month/year/force cursor, OSI spinner *and its list overlay*, submit/cancel), `argv_builder.py`, `render.py` (raw ANSI, no color library) and the refresh screen's `refresh_state.py`/`refresh_render.py` import nothing from `prompt_toolkit` and are plain-pytest tested. The overlay is a mode of the same state (`OsiSpinner.picking`), not a second `Application`: on the OSI row Tab/Space/Enter open the list, arrows move (inverted — in a list Down means the row below, not "one less"), Space/Enter choose, Tab/Esc go back unchanged, and Ctrl-C always leaves. `app.py` is the only file that imports `prompt_toolkit` (it holds both screens' builders); it's tested by driving the real `Application` with `prompt_toolkit.input.create_pipe_input()`/`prompt_toolkit.output.DummyOutput` (synthetic key sequences), not a real terminal — this project's tools cannot open a genuine Win32 console, and manual verification in a real PowerShell/cmd window or the double-clicked `.exe` is the only way to confirm the visuals.
- **OSI catalog is two separate files under `user_data_dir()`.** `osi_catalog.json` (the list, written only by `--refresh-osi-list`) and `osi_last_used.json` (written only by `punch()`, right after a real save succeeds — `Appointment.osi`/`history.csv` still don't record which OSI was used, so this is the only source of "last used"; it stores `osi_label`, and still reads the `osi_number` an older build wrote). `probe.py` must import the browser stack **lazily** inside `refresh_catalog()`: `modules/osi_catalog/__init__.py` imports it at module level and the browser-free TUI imports the package.
- **Both OSI lists are read through the site's own API, not the DOM** (`modules/browser/ssg_api.py`; contract in `doc/sysmap_ssg/api.md`, probed 2026-09-10). The `/api/<id>/` route segment is the **`AspSession` cookie**, and calls must go through the page (`CdpPage.fetch_json`) because the session is a cookie in the browser profile. `ReturnCode` `1001` is success (an empty list included — a legitimate answer, not a failure); `INVALID_TOKEN*` means the session died and raises `SsgLoginRequired`; anything else raises `SsgApiUnavailable`, which is the signal to fall back to driving the screen. **`userName` is mandatory**: the OSI endpoint answers a blank one with success and zero items, so `logged_user()` refuses an empty `.user-name` rather than write an empty catalog.
- **The catalog keeps what it cannot punch; the form does not offer it.** `OsiSpinner.known` holds everything captured and `OsiSpinner.entries` is the view of it for the day the form is on, recomputed by `refilter()` whenever the date changes (a cancelled OSI, or one whose window closed, is simply not offered for that day). A day with a non-empty catalog but nothing valid says so, instead of telling you to run `refresh-osi-list`.
- **The two sources are complementary, and a source only overwrites itself.** `apontamento` (the day's "?" list) is the only one that sees the OSIs a manager opens for the whole team; `listagem` (`osi/get-by-parameters`) is the only one that carries `StatusName` and the real validity window. They merge by number into one `osi_catalog.json` (`cache.merge_entries`), where the label the timesheet actually listed wins as the selector and the status/window come from the listing. **`Liberado` is not enough to be punchable**: on 2026-09-10, 24 of 30 OSIs were `Liberado` and only 4 were valid that day, so `punchable()` also demands the day be inside `OsiActivityStartDateStr..EndDateStr`.
- **Which day's list gets read is decided from the site's own words.** The day panel's heading carries the weekday in caps and writes **`FERIADO`** in its place — the only source that knows a company or regional holiday. `SsgController.days_on_screen()` reads it raw; `modules/osi_catalog/day_choice.py` (pure, browser-free, tested) turns it into the candidate ladder, newest first, **today included**. `--incluir-feriado`/`--incluir-fim-de-semana` widen it. An empty list from a day is a reason to step down the ladder, not to fail, and the day that answered is printed and stored.
- **`filter_range` proves it filtered.** It verifies each typed date by reading the field back (the mask turned `31072026` into `31/07/261` on 2026-09-10), clears a **stale** site alert before acting and turns a **new** one into `SsgAlert` carrying the site's own text, and waits for a panel that did not exist before the click (`data-aa-stale` marker) — waiting for "the panel of day X" alone matches the *previous* filter's identical panel. That combination is what let a filter silently not happen. Fixed waits are gone from the read path (the filter answers in ~1.3s); the masked-field typing recipe in `cdp.py` and `_AFTER_SAVE_SECONDS` are deliberately untouched.
- **The listed OSI text is free text, and the label is the identity.** `OsiEntry.from_label` never raises: an OSI of your own reads `OSI <numero> | <Projeto> | <Atividade> - <id>`, but the ones a manager opens for the whole team carry no number at all (`coe tech - setembro - 2026 | Walber Hugo da Silva - 427465`), and everything the modal offers for a day is punchable that day — so refusing a row would throw away a usable OSI. `number` stays a mandatory field and holds the sentinel `NUMBER_NOT_CAPTURED` ("NÃO CAPTURADO") when there is no prefix: a visible trace in the JSON, never a selector. Everything that selects an OSI (`--osi`, the TUI, `_fill_osi`) travels by `label`.
- **There is no default OSI.** A number written into the code goes stale the month after (82695 was dead by 30/08). `punch()` uses `--osi`, then `load_last_used()`, then refuses; the TUI blocks submitting when it has neither catalog nor last-used, so nothing opens a browser only to fail there.

### Browser layer
`modules/browser/` defines `BrowserController` (ABC: `open` / `login` / `fill_appointment` / `close`). `SsgController` is the real implementation; `PlaywrightController` and `InputController` remain stubs that raise `NotImplementedError` (`InputController` is the intended second implementation, for the comparison the project always planned).

- **We never ship a browser.** `browsers.py` drives the Edge (falling back to Chrome) already installed, over CDP, using a dedicated persistent profile under `user_data_dir()`. Whichever browser answered is remembered in `browser.json`. Keeps the future `.exe` at ~15 MB.
- **Login is never automated.** The portal asks for a Google Authenticator code. Landing on a login page raises `SsgLoginRequired`; the user signs in by hand once and the profile keeps the session.
- **Filling never saves.** `fill_appointment` types and verifies; `save_day` is a separate call. Every write to a real timekeeping system leaves a trace, so it must be deliberate. `verify` raises `FieldMismatch` before any save can happen.
- **The OSI is picked from the day's "?" list, never typed.** `_fill_osi` opens the same modal the catalog refresh reads, matches the requested label (`ssg_list_modal.match_row`: exact, then a unique substring — ambiguity raises), clicks that row's `button.button-select` and reads the field back into `controller.osi_selected`. `verify` then demands that exact string (whitespace collapsed). The typeahead is gone: it needs a number the manager's OSIs don't have, and it clicked "the first visible `li`" blind. `--osi` is therefore free text — a bare `82695` still works, as a unique substring of its own row.
- **A minimised or covered window freezes the site's modals.** Chromium reports `document.visibilityState === "hidden"`, CSS transitions stop, and a Bootstrap modal never finishes opening or closing — "Fechar" looks dead even though the click lands on the button. `browsers.launch` passes `--disable-backgrounding-occluded-windows` (plus the renderer/timer ones) to prevent it; `Page.bringToFront` does not help. Against a browser someone else started the flags are absent, so `ssg_list_modal` appends a hint naming this when a modal will not open or close and `document.hidden` is true. Measured 2026-09-09; details in `doc/sysmap_ssg/stack.md`.
- **`suppress_origin=True` is load-bearing** in `cdp.py`: `websocket-client` sends an `Origin` header and Chromium answers CDP handshakes carrying one with **403**, unless the browser was launched with `--remote-allow-origins`. Dropping the header works against a browser someone else started, without loosening the browser.
- **Site selectors and traps live in `doc/sysmap_ssg/pages/<tela>/selectors.json`** (narrative in each folder's `README.md`), not next to the controllers — it's recon knowledge, not something any code imports. Nothing on those screens has an `id` or `name`; everything is anchored by class. The constants in `ssg_controller.py` (timesheet) and `ssg_osi_form.py` (OSI form) are the executable copies — change both together.
- **`ssg_screen.py` owns the shared lifecycle** (`SsgScreen`: `open`/`close`/`require_session`/`clear_stale_alert`/`fail_on_alert`, and the `SsgError` family — `ssg_controller.py` re-exports them); `ssg_list_modal.py` owns the site's generic "Listagem de Itens" widget that every "?" button opens (`wait_open`/`rows`/`select`/`close`); `ssg_api.py` owns the site's service layer (the fast path for both OSI lists); `ssg_osi_list.py` drives `#/osi/get-list` as that path's fallback. New screens subclass `SsgScreen` and set `url` + `ready_selector`.
- **A click that would not reach its target raises instead of doing nothing.** `CdpPage.click` checks `document.elementFromPoint` first and raises `ClickBlocked` naming what is in the way. This site produces invisible blockers three ways: the **Bootbox alert** (a stale one is read, reported and dismissed by `clear_stale_alert`, since otherwise the tool is unusable until a human clicks OK; one that appears in *response* to our action raises `SsgAlert` with the site's own words); the **stuck loading veil** (`runWebMethod` raises a classless full-screen `position: fixed` div at `z-index: 10000` and only drops it in the response callback, so a request that never returns leaves it up — `clear_stale_overlay` lifts it with the site's own `hideLoading`); and a **poisoned tab** (measured 2026-09-11: at `jQuery.active == 6` the tab stopped firing *any* request, and a full reload did not cure it while a fresh tab on the same profile was fine — `stuck_hint()` says so in the error text).
